interface LoaderConfig {
    provider: any;
    defaultSender: string;
    defaultGas: number;
}
export declare function setupLoader({ provider, defaultSender, defaultGas }: LoaderConfig): {
    web3: {
        fromABI: (abi: object, bytecode?: string) => any;
        fromArtifact: (contract: string) => any;
    };
    truffle: {
        fromABI: (abi: object, bytecode?: string) => any;
        fromArtifact: (contract: string) => any;
    };
};
export {};
