"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ganache_core_1 = __importDefault(require("ganache-core"));
function send(msg) {
    if (process.send === undefined) {
        throw new Error('Module must be started through child_process.fork');
    }
    process.send(msg);
}
function setupServer({ accountsConfig, gasLimit, gasPrice, coverage }) {
    const ganacheOpts = {
        accounts: accountsConfig,
        gasLimit,
        gasPrice: `0x${gasPrice.toString(16)}`,
    };
    if (!coverage) {
        return ganache_core_1.default.server(ganacheOpts);
    }
    else {
        return require('ganache-core-coverage').server(Object.assign(Object.assign({}, ganacheOpts), { emitFreeLogs: true, allowUnlimitedContractSize: true }));
    }
}
process.once('message', (options) => {
    const server = setupServer(options);
    // An undefined port number makes ganache-core choose a random free port,
    // which plays nicely with environments such as jest and ava, where multiple
    // processes of test-environment may be run in parallel.
    // It also means however that the port (and therefore host URL) is not
    // available until the server finishes initialization.
    server.listen(undefined, function (err) {
        if (err) {
            send({ type: 'error' });
        }
        else {
            send({ type: 'ready', port: server.address().port });
        }
    });
    process.on('disconnect', () => {
        server.close();
    });
});
//# sourceMappingURL=ganache-server.js.map